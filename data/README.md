# Data

Shared input data used by the notebooks lives here.

`the-verdict.txt` is the text corpus used for the early tokenizer, training
window, and embedding exercises. Keep notebooks independent of duplicated data
files by loading this copy through the repository root.
